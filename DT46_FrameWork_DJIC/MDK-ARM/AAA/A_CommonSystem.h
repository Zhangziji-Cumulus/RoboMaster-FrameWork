//** #################################################################################################### **//
//** ======================================== STM32头文件库 =============================================== **//
//** #################################################################################################### **//

#include "main.h"
#include "freertos.h"
#include "cmsis_os.h"
#include "can.h"
#include "usart.h"

//** #################################################################################################### **//
//** ======================================== 常用库头文件 =============================================== **//
//** #################################################################################################### **//

#include <stdint.h> //固定宽度整数类型
// 用途: 定义精确定义大小的整数类型，如 uint8_t, int16_t, uint32_t。
// 常用示例: uint32_t tickCount; int16_t sensorValue;

#include "math.h" //数学函数库
// 用途: 提供数学函数，如 sin(), cos(), sqrt(), fabs()。
// 常用示例: float angle = sinf(theta); float dist = sqrtf(x*x + y*y);

#include <stdlib.h> //标准通用工具库
// 用途: 提供通用工具函数，如 malloc(), free(), abs(), rand()。
// 常用示例: ptr = malloc(size); int r = rand() % 100;

#include <stdbool.h> //布尔类型支持
// 用途: 提供 bool, true, false 等布尔类型支持。
// 常用示例: bool isReady = true; if (!isReady) { ... }

#include <string.h> //字符串处理函数
// 用途: 提供字符串处理函数，如 memcpy(), memset(), strcmp()。  
// 常用示例: char buffer[20]; memset(buffer, 0, sizeof(buffer)); memcpy(dest, src, len);

